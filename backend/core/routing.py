# from django.urls import re_path
#
# from apps.common.consumers import GlobalConsumer
# from apps.users.consumers import AuthenticatedConsumer
#
# websocket_urlpatterns = [
#     re_path(r"ws/global/$", GlobalConsumer.as_asgi()),
#     re_path(r"ws/user/$", AuthenticatedConsumer.as_asgi()),
# ]
